import random


def is_relevant(text):
    """Placeholder classifier that randomly returns True or False.
    To be replaced by the real classifier team.

    Args:
        text (str): The comment text to classify.

    Returns:
        bool: True if the comment is relevant, False otherwise.
    """
    return random.choice([True, False])